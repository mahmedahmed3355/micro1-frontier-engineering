{
  "candidate_files": [
    "candidate.cu"
  ],
  "candidate_sha256": "1560e0c15589c65c08bc291151a49c0af4a6e454782e3110405e54787ea0b2ba",
  "candidate_source": "#include <cuda_runtime.h>\n#include <stdio.h>\n#include <stdlib.h>\n#include <algorithm>\n#include <vector>\n\n#define BM 64\n#define BN 64\n#define BK 16\n#define TM 4\n#define TN 4\n\n// Tiled Matrix Multiplication Kernel (C = A * B)\n// Handles arbitrary N x N matrices with boundary protection\n__global__ void __launch_bounds__(256) gemm_kernel(\n    const float* __restrict__ A,\n    const float* __restrict__ B,\n    float* __restrict__ C,\n    int N)\n{\n    __shared__ float s_A[BM][BK + 1];\n    __shared__ float s_B[BK][BN + 1];\n\n    int tx = threadIdx.x; // 0..15\n    int ty = threadIdx.y; // 0..15\n    int tid = ty * 16 + tx; // 0..255\n\n    int bx = blockIdx.x;\n    int by = blockIdx.y;\n\n    float accum[TM][TN] = {0.0f};\n\n    int num_tiles = (N + BK - 1) / BK;\n\n    for (int t = 0; t < num_tiles; ++t) {\n        int k_base = t * BK;\n\n        // Load s_A tile into shared memory (64x16 floats, 256 threads -> 4 per thread)\n        #pragma unroll\n        for (int i = 0; i < 4; ++i) {\n            int a_row_local = (tid / 16) + i * 16;\n            int a_col_local = tid % 16;\n            int a_row_global = by * BM + a_row_local;\n            int a_col_global = k_base + a_col_local;\n\n            if (a_row_global < N && a_col_global < N) {\n                s_A[a_row_local][a_col_local] = A[a_row_global * N + a_col_global];\n            } else {\n                s_A[a_row_local][a_col_local] = 0.0f;\n            }\n        }\n\n        // Load s_B tile into shared memory (16x64 floats, 256 threads -> 4 per thread)\n        #pragma unroll\n        for (int i = 0; i < 4; ++i) {\n            int b_row_local = (tid + i * 256) / BN;\n            int b_col_local = (tid + i * 256) % BN;\n            int b_row_global = k_base + b_row_local;\n            int b_col_global = bx * BN + b_col_local;\n\n            if (b_row_global < N && b_col_global < N) {\n                s_B[b_row_local][b_col_local] = B[b_row_global * N + b_col_global];\n            } else {\n                s_B[b_row_local][b_col_local] = 0.0f;\n            }\n        }\n\n        __syncthreads();\n\n        // Compute sub-tile output in registers\n        #pragma unroll\n        for (int k = 0; k < BK; ++k) {\n            float a_reg[TM];\n            float b_reg[TN];\n\n            #pragma unroll\n            for (int i = 0; i < TM; ++i) {\n                a_reg[i] = s_A[ty * TM + i][k];\n            }\n\n            #pragma unroll\n            for (int j = 0; j < TN; ++j) {\n                b_reg[j] = s_B[k][tx * TN + j];\n            }\n\n            #pragma unroll\n            for (int i = 0; i < TM; ++i) {\n                #pragma unroll\n                for (int j = 0; j < TN; ++j) {\n                    accum[i][j] += a_reg[i] * b_reg[j];\n                }\n            }\n        }\n\n        __syncthreads();\n    }\n\n    // Write computed results to global memory C\n    #pragma unroll\n    for (int i = 0; i < TM; ++i) {\n        int c_row = by * BM + ty * TM + i;\n        #pragma unroll\n        for (int j = 0; j < TN; ++j) {\n            int c_col = bx * BN + tx * TN + j;\n            if (c_row < N && c_col < N) {\n                C[c_row * N + c_col] = accum[i][j];\n            }\n        }\n    }\n}\n\nint main(int argc, char** argv) {\n    int N = 1024;\n    if (argc > 1) {\n        N = atoi(argv[1]);\n    }\n    if (N <= 0) N = 1024;\n\n    size_t num_elements = (size_t)N * N;\n    size_t bytes = num_elements * sizeof(float);\n\n    float* h_A = (float*)malloc(bytes);\n    float* h_B = (float*)malloc(bytes);\n    float* h_C = (float*)malloc(bytes);\n\n    // Deterministic random generation with seed 12345 in range [-1.0, 1.0]\n    srand(12345);\n    for (size_t i = 0; i < num_elements; ++i) {\n        h_A[i] = ((float)rand() / (float)RAND_MAX) * 2.0f - 1.0f;\n    }\n    for (size_t i = 0; i < num_elements; ++i) {\n        h_B[i] = ((float)rand() / (float)RAND_MAX) * 2.0f - 1.0f;\n    }\n\n    float *d_A = nullptr, *d_B = nullptr, *d_C = nullptr;\n    cudaMalloc((void**)&d_A, bytes);\n    cudaMalloc((void**)&d_B, bytes);\n    cudaMalloc((void**)&d_C, bytes);\n\n    cudaMemcpy(d_A, h_A, bytes, cudaMemcpyHostToDevice);\n    cudaMemcpy(d_B, h_B, bytes, cudaMemcpyHostToDevice);\n\n    dim3 blockDim(16, 16);\n    dim3 gridDim((N + BN - 1) / BN, (N + BM - 1) / BM);\n\n    // Warm-up run\n    gemm_kernel<<<gridDim, blockDim>>>(d_A, d_B, d_C, N);\n    cudaDeviceSynchronize();\n\n    const int NUM_RUNS = 100;\n    std::vector<float> times(NUM_RUNS);\n\n    cudaEvent_t start, stop;\n    cudaEventCreate(&start);\n    cudaEventCreate(&stop);\n\n    for (int r = 0; r < NUM_RUNS; ++r) {\n        cudaEventRecord(start);\n        gemm_kernel<<<gridDim, blockDim>>>(d_A, d_B, d_C, N);\n        cudaEventRecord(stop);\n        cudaEventSynchronize(stop);\n\n        float ms = 0.0f;\n        cudaEventElapsedTime(&ms, start, stop);\n        times[r] = ms;\n    }\n\n    cudaMemcpy(h_C, d_C, bytes, cudaMemcpyDeviceToHost);\n\n    std::sort(times.begin(), times.end());\n    float median_time = (times[49] + times[50]) / 2.0f;\n\n    printf(\"SIZE=%d\\n\", N);\n    printf(\"KERNEL_TIME_MS=%.6f\\n\", median_time);\n\n    printf(\"RESULT_SAMPLE=\");\n    int sample_count = (num_elements < 10) ? (int)num_elements : 10;\n    for (int i = 0; i < 10; ++i) {\n        int idx = (i < sample_count) ? i : 0;\n        printf(\"%s%.6f\", (i > 0 ? \",\" : \"\"), h_C[idx]);\n    }\n    printf(\"\\n\");\n\n    cudaEventDestroy(start);\n    cudaEventDestroy(stop);\n    cudaFree(d_A);\n    cudaFree(d_B);\n    cudaFree(d_C);\n    free(h_A);\n    free(h_B);\n    free(h_C);\n\n    return 0;\n}",
  "generation_error": "",
  "optimization_history": [
    {
      "agent": "optimizer",
      "iteration": 0,
      "response": "```cuda\n#include <cuda_runtime.h>\n#include <stdio.h>\n#include <stdlib.h>\n#include <algorithm>\n#include <vector>\n\n#define BM 64\n#define BN 64\n#define BK 16\n#define TM 4\n#define TN 4\n\n// Tiled Matrix Multiplication Kernel (C = A * B)\n// Handles arbitrary N x N matrices with boundary protection\n__global__ void __launch_bounds__(256) gemm_kernel(\n    const float* __restrict__ A,\n    const float* __restrict__ B,\n    float* __restrict__ C,\n    int N)\n{\n    __shared__ float s_A[BM][BK + 1];\n    __shared__ float s_B[BK][BN + 1];\n\n    int tx = threadIdx.x; // 0..15\n    int ty = threadIdx.y; // 0..15\n    int tid = ty * 16 + tx; // 0..255\n\n    int bx = blockIdx.x;\n    int by = blockIdx.y;\n\n    float accum[TM][TN] = {0.0f};\n\n    int num_tiles = (N + BK - 1) / BK;\n\n    for (int t = 0; t < num_tiles; ++t) {\n        int k_base = t * BK;\n\n        // Load s_A tile into shared memory (64x16 floats, 256 threads -> 4 per thread)\n        #pragma unroll\n        for (int i = 0; i < 4; ++i) {\n            int a_row_local = (tid / 16) + i * 16;\n            int a_col_local = tid % 16;\n            int a_row_global = by * BM + a_row_local;\n            int a_col_global = k_base + a_col_local;\n\n            if (a_row_global < N && a_col_global < N) {\n                s_A[a_row_local][a_col_local] = A[a_row_global * N + a_col_global];\n            } else {\n                s_A[a_row_local][a_col_local] = 0.0f;\n            }\n        }\n\n        // Load s_B tile into shared memory (16x64 floats, 256 threads -> 4 per thread)\n        #pragma unroll\n        for (int i = 0; i < 4; ++i) {\n            int b_row_local = (tid + i * 256) / BN;\n            int b_col_local = (tid + i * 256) % BN;\n            int b_row_global = k_base + b_row_local;\n            int b_col_global = bx * BN + b_col_local;\n\n            if (b_row_global < N && b_col_global < N) {\n                s_B[b_row_local][b_col_local] = B[b_row_global * N + b_col_global];\n            } else {\n                s_B[b_row_local][b_col_local] = 0.0f;\n            }\n        }\n\n        __syncthreads();\n\n        // Compute sub-tile output in registers\n        #pragma unroll\n        for (int k = 0; k < BK; ++k) {\n            float a_reg[TM];\n            float b_reg[TN];\n\n            #pragma unroll\n            for (int i = 0; i < TM; ++i) {\n                a_reg[i] = s_A[ty * TM + i][k];\n            }\n\n            #pragma unroll\n            for (int j = 0; j < TN; ++j) {\n                b_reg[j] = s_B[k][tx * TN + j];\n            }\n\n            #pragma unroll\n            for (int i = 0; i < TM; ++i) {\n                #pragma unroll\n                for (int j = 0; j < TN; ++j) {\n                    accum[i][j] += a_reg[i] * b_reg[j];\n                }\n            }\n        }\n\n        __syncthreads();\n    }\n\n    // Write computed results to global memory C\n    #pragma unroll\n    for (int i = 0; i < TM; ++i) {\n        int c_row = by * BM + ty * TM + i;\n        #pragma unroll\n        for (int j = 0; j < TN; ++j) {\n            int c_col = bx * BN + tx * TN + j;\n            if (c_row < N && c_col < N) {\n                C[c_row * N + c_col] = accum[i][j];\n            }\n        }\n    }\n}\n\nint main(int argc, char** argv) {\n    int N = 1024;\n    if (argc > 1) {\n        N = atoi(argv[1]);\n    }\n    if (N <= 0) N = 1024;\n\n    size_t num_elements = (size_t)N * N;\n    size_t bytes = num_elements * sizeof(float);\n\n    float* h_A = (float*)malloc(bytes);\n    float* h_B = (float*)malloc(bytes);\n    float* h_C = (float*)malloc(bytes);\n\n    // Deterministic random generation with seed 12345 in range [-1.0, 1.0]\n    srand(12345);\n    for (size_t i = 0; i < num_elements; ++i) {\n        h_A[i] = ((float)rand() / (float)RAND_MAX) * 2.0f - 1.0f;\n    }\n    for (size_t i = 0; i < num_elements; ++i) {\n        h_B[i] = ((float)rand() / (float)RAND_MAX) * 2.0f - 1.0f;\n    }\n\n    float *d_A = nullptr, *d_B = nullptr, *d_C = nullptr;\n    cudaMalloc((void**)&d_A, bytes);\n    cudaMalloc((void**)&d_B, bytes);\n    cudaMalloc((void**)&d_C, bytes);\n\n    cudaMemcpy(d_A, h_A, bytes, cudaMemcpyHostToDevice);\n    cudaMemcpy(d_B, h_B, bytes, cudaMemcpyHostToDevice);\n\n    dim3 blockDim(16, 16);\n    dim3 gridDim((N + BN - 1) / BN, (N + BM - 1) / BM);\n\n    // Warm-up run\n    gemm_kernel<<<gridDim, blockDim>>>(d_A, d_B, d_C, N);\n    cudaDeviceSynchronize();\n\n    const int NUM_RUNS = 100;\n    std::vector<float> times(NUM_RUNS);\n\n    cudaEvent_t start, stop;\n    cudaEventCreate(&start);\n    cudaEventCreate(&stop);\n\n    for (int r = 0; r < NUM_RUNS; ++r) {\n        cudaEventRecord(start);\n        gemm_kernel<<<gridDim, blockDim>>>(d_A, d_B, d_C, N);\n        cudaEventRecord(stop);\n        cudaEventSynchronize(stop);\n\n        float ms = 0.0f;\n        cudaEventElapsedTime(&ms, start, stop);\n        times[r] = ms;\n    }\n\n    cudaMemcpy(h_C, d_C, bytes, cudaMemcpyDeviceToHost);\n\n    std::sort(times.begin(), times.end());\n    float median_time = (times[49] + times[50]) / 2.0f;\n\n    printf(\"SIZE=%d\\n\", N);\n    printf(\"KERNEL_TIME_MS=%.6f\\n\", median_time);\n\n    printf(\"RESULT_SAMPLE=\");\n    int sample_count = (num_elements < 10) ? (int)num_elements : 10;\n    for (int i = 0; i < 10; ++i) {\n        int idx = (i < sample_count) ? i : 0;\n        printf(\"%s%.6f\", (i > 0 ? \",\" : \"\"), h_C[idx]);\n    }\n    printf(\"\\n\");\n\n    cudaEventDestroy(start);\n    cudaEventDestroy(stop);\n    cudaFree(d_A);\n    cudaFree(d_B);\n    cudaFree(d_C);\n    free(h_A);\n    free(h_B);\n    free(h_C);\n\n    return 0;\n}\n```"
    }
  ]
}
