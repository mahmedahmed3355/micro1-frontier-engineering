# Defective Implementation

This directory contains the intentionally defective implementation for Case 006.

The implementation models a PyTorch CUDA device mismatch. The defect must remain intentional so that the benchmark evaluates whether an agent can identify and correct the device-placement problem.

Do not modify the defect merely to make static tests pass.
