# Case Tests

The tests in this directory validate the structural contract of Case 006.

The contract checks device selection, tensor placement, and the ordering of device preparation relative to the computation.

Tests must remain deterministic and must not require a live Gemini API call.
