# Reference Implementation

This directory contains the correct reference implementation for Case 006.

The reference establishes a compatible execution device and ensures that the participating tensors required by the computation are placed on that device before the operation.

The reference exists as the behavioral oracle for the case.
