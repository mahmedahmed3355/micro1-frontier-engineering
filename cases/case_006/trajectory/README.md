# Case 006 Trajectory

This directory is reserved for trajectory artifacts generated during agent execution.

Trajectory records should preserve the sequence of agent interactions, including inspected context, reasoning outputs, candidate changes, verification results, and rejected attempts when the execution pipeline records them.

The directory must remain available even before a live execution trajectory is produced.
