# Reference Implementation

This directory contains the behavioral reference for Case 007.

The reference explicitly coordinates the distributed workers before the optimization boundary and performs deterministic loss aggregation across the participating workers.

The reference is the oracle for the intended synchronization contract.
