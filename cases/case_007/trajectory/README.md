# Case 007 Trajectory

This directory is reserved for trajectory artifacts generated during benchmark execution.

Recorded trajectories should preserve the agent interaction sequence, inspected context, candidate changes, verification results, and rejected attempts when the execution pipeline records them.
