# Case 003 Trajectory

This directory stores execution trajectory artifacts for Case 003.

Runtime trajectory records should capture the case identifier,
implementation identity, workload, stream configuration, verification
result, and relevant execution metadata.

Trajectory recording must not change scheduling, synchronization, or
candidate behavior.

No runtime trajectory is expected during the static preparation phase.
